OS_Windows = "Windows"
OS_Linux = "Linux"

CMD_RUN = "run"
CMD_LOAD_RUNTIME = "load"
CMD_INSTALL_RUNTIME = "install"
CMD_LAUNCH_APP = "launch"
CMD_BUILD_RUNTIME = "build"

ARG_APPRUNNER_WORKING_ROOT = "--apprunner-working-root"
ARG_APPRUNNER_DATA_ROOT = "--apprunner-data-root"

ARG_HOST_PLATFORM = "--host-platform"
ARG_APP_TYPE = "--app-type"
ARG_RUNTIME_BINARY = "--runtime-binary"
ARG_RUNTIME_NAME = "--runtime-name"
ARG_RUNTIME_VERSION = "--runtime-version"
ARG_RUNTIME_SOURCE = "--runtime-source"
ARG_SETUP_ONLY = "--setup-only"
ARG_APP_ARGS = "--app-args"

ARG_BUILD_SOURCE = "--build-source"
ARG_ARCHITECTURES = "--architectures"
ARG_LOCAL_PACKAGE_DIR = "--package-dir"
ARG_PUBLISH_DIR = "--publish-dir"
ARG_USE_TEMP_BUILD_DIR = "--use-temp-build-dir"
ARG_RESERVE_BUILD_DIR = "--reserve-build-dir"
ARG_COPY_SITE_PACKAGES = "--copy-site-packages"

ARG_INSTALL_SOURCE = "--install-source"
ARG_INSTALL_TARGET = "--install-target"

APPRUNNER_COSMOS_ROOT = "https://cosmos08.osdinfra.net/cosmos/cosmos.upload/shares/bingads.data.clickpredict/data/ClickPrediction/TrainStation/AppRunner/"
APPRUNNER_SCRIPT = "apprunner.py"

PHILLY = "Philly"
TRAINSTATION = "TrainStation"
PAI = "PAI"

VERSIONS_INI = "versions.ini"
LATEST = "20180814"

RUNTIME_INI = "runtime.ini"
RUNTIME_ZIP_NAME = "runtime.zip"
RUNTIME_STAGING_DIR_NAME = "runtime_staging"
RUNTIME_DIR_NAME = "runtime"
RUNTIME_INSTALL_SCRIPT_NAME = "install.py"

REQUIREMENTS_TXT = "requirements.txt"
CONDA_SPEC_TXT = "conda_spec.txt"

PYTHON_DIST_CPYTHON = "CPython"
PYTHON_DIST_ANACONDA = "Anaconda"
